package main;

/* This is the driver that will build everything. 
 * The MainFrame will take care of building everything 
 * for the display. */

public class Driver {

	public static void main(String[] args) {
		//create a new frame
		MainFrame frame = new MainFrame();
		frame.setVisible( true );
	}

}
